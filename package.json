{
  "name": "ark-tracking-evolved-api",
  "version": "1.0.0",
  "description": "GraphQL API for the Ark: Tracking Evolved app",
  "main": "dist/app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prebuild": "tslint -c tslint.json -p tsconfig.json --fix",
    "build": "tsc",
    "prestart": "npm run build",
    "start": "env NODE_ENV=development && node ."
  },
  "author": "Zachary Hall",
  "license": "ISC",
  "dependencies": {
    "@types/config": "0.0.36",
    "config": "^3.3.0",
    "express": "^4.18.2",
    "express-graphql": "^0.9.0",
    "graphql": "^14.6.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.3",
    "@types/node": "^13.9.0",
    "tslint": "^6.0.0",
    "typescript": "^3.8.3"
  }
}
